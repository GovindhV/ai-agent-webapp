# 1. Install LangFlow
pip install langflow
langflow run

# 2. Create your project
mkdir ai-agent-webapp && cd ai-agent-webapp

# 3. Copy the files from artifacts
# 4. Install dependencies
npm install

# 5. Update the Flow ID in server.js
# 6. Start the app
npm start
